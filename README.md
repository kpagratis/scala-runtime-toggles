# scala-runtime-toggles
Runtime toggles allow users to dynamically change the
behavior of their code without having to restart/redeploy.

Toggles are loaded from a yaml file and are re-read on a schedule